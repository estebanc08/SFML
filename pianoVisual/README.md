# TinyXML2

TinyXML2 is a simple, small, efficient, C++ XML parser. Used to parse MusicXML into text file I can parse.

## Download

```sh
git clone https://github.com/leethomason/tinyxml2.git
cd tinyxml2
mkdir build
cd build
cmake ..
make
sudo make install
